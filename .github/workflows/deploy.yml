name: Deploy to EC2 via SSH

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    # Remove or comment out the environment line if you don’t have secrets set there
    # environment: production

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up SSH
        env:
          DEPLOY_KEY: ${{ secrets.DEPLOYKEY }}
          HOST: ${{ secrets.HOST }}
        run: |
          echo "DEPLOY_KEY length: ${#DEPLOY_KEY}"
          echo "HOST is: '$HOST'"
          if [ -z "$HOST" ]; then
            echo "ERROR: HOST secret is empty or missing!"
            exit 1
          fi
          mkdir -p ~/.ssh
          printf "%s" "$DEPLOY_KEY" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H "$HOST" >> ~/.ssh/known_hosts

      - name: Deploy to EC2
        env:
          USER: ${{ secrets.USER }}
          HOST: ${{ secrets.HOST }}
        run: |
          ssh -o StrictHostKeyChecking=no $USER@$HOST << 'EOF'
            cd /home/ec2-user/aws-test-project
            git pull origin main
            npm install
            pm2 restart next-app || pm2 start npm --name "next-app" -- run start -- -H 0.0.0.0
          EOF
